export enum AppRoute {
    Home = '/',
    SupportedCurrencyPairs = '/supported-currency-pairs',
    Orders = '/orders',
    Create = '/orders/create',
  }

  export enum RouteName {
    SupportedCurrencyPairs = 'Supported Currency Pairs',
    Orders = 'Orders',
  }
  